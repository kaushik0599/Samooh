/**
 * PLACEHOLDER ABI — not sourced from a deployed contract.
 *
 * The blockchain team must replace this array with the real Governance
 * contract ABI once available (see docs/BLOCKCHAIN_INTEGRATION.md for the
 * exact function/event signatures this backend expects). Until then, every
 * call through GovernanceAdapter fails with a clear "not configured" style
 * error instead of returning fabricated data.
 */
export const GOVERNANCE_ABI: readonly string[] = [];
