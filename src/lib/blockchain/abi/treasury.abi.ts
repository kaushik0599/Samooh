/**
 * PLACEHOLDER ABI — not sourced from a deployed contract.
 *
 * The blockchain team must replace this array with the real Treasury
 * contract ABI once available (see docs/BLOCKCHAIN_INTEGRATION.md). Until
 * then, every call through TreasuryAdapter fails with a clear
 * "not configured" style error instead of returning fabricated data.
 */
export const TREASURY_ABI: readonly string[] = [];
